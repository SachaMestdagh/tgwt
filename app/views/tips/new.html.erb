new.html.erb

<div class="accountbg d-flex align-items-end justify-content-center flex-column" id="accountbg"> </div>


 <div class="inputforms">
<%= simple_form_for(@tip, html: { multipart: true }) do |f| %>

    <% if @tip.errors.any? %>
    <div class="errors-container">
      <ul>
        <% @tip.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

  <div class="form-inputs">
    <%= f.input :title,
       label: false,
       placeholder: 'Title' %>
       <%= f.input :summary,
       label: false,
       placeholder: 'Summary' %>
       <%= f.input :content,
       label: false,
       placeholder: 'Content',
       :as => :text %>
       <h5>Add pictures</h5>
      <%= f.file_field :tip_photos, multiple: true, accept: "image/jpeg, image/jpg, image/gif, image/png", input_html: { name: 'Photos', label: 'Photos' } %>
      <%= f.button :submit, 'Add a Tip.' , class: "addamealbtn mb-3", style: "width: 80%;" %>
  </div>
<% end %>

</div>
